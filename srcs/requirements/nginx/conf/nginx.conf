events {}

http {
    server {
        listen       443 ssl;
        server_name  84.8.216.105;
        root         /usr/share/nginx/html;

        ssl_certificate     /etc/nginx/ssl/server.crt;
        ssl_certificate_key /etc/nginx/ssl/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
    }
}